(ns anketeur.test.controller.result
  (:require [clojure.test :refer :all]
            [anketeur.controller.result :refer :all]))

(deftest test-export
  (testing "export renders data"
    (is (= 1 1)))) 
